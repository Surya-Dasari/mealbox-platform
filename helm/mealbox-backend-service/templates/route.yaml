apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "mealbox.fullname" . }}
spec:
  to:
    kind: Service
    name: {{ include "mealbox.fullname" . }}
  port:
    targetPort: {{ .Values.service.port }}
  tls:
    termination: edge

