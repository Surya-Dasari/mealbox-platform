apiVersion: v1
kind: Service
metadata:
  name: {{ include "mealbox.fullname" . }}
spec:
  selector:
    app: {{ include "mealbox.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}

