app:
  name: frontend-web

image:
  repository: suryadasari31/mealbox-frontend-web
  tag: latest
  pullPolicy: IfNotPresent

container:
  port: 80

service:
  type: ClusterIP
  port: 80

route:
  enabled: true
  host: ""

resources:
  requests:
    cpu: 10m
    memory: 64Mi
  limits:
    cpu: 500m
    memory: 256Mi

env: []

